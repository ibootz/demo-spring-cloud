spring:
  application:
    name: DEMO-ZUUL
  cloud:
    config:
      profile: default
      label: master
      discovery:
        enabled: true
        service-id: DEMO-CONFIG
server:
  port: 8261
eureka:
  client:
    serviceUrl:
      defaultZone: http://peer1:8761/demo/eureka/,http://peer2:8762/demo/eureka/